# [系统/模块]「[需求名称]」开发任务

> 状态：待开发
> 适用范围：[系统、帐套、模块]
> 不适用范围：[明确排除的系统、帐套、模块]
> 核心目标：[一句话说明业务结果]
> 实现边界：[现有语言、架构和禁止扩展的范围]
> 勾选说明：第 1～2 节 `[x]` 表示业务规则或项目范围已经确认；第 3 节以后 `[ ]` 表示开发工作尚未实施

---

## 1. 已确认的业务规则

### 1.1 现行规则与目标规则

[描述现行逻辑、目标逻辑、字段含义和数据来源。组合键使用代码块。]

### 1.2 配置及兼容矩阵

| 配置 A | 配置 B | 实际行为/维度 | 字段写入规则 |
|--------|--------|---------------|----------------|
| 否 | 否 | [旧模式] | [未启用字段写入规则] |
| 是 | 否 | [模式] | [规则] |
| 否 | 是 | [模式] | [规则] |
| 是 | 是 | [完整模式] | [规则] |

- [x] [已确认的配置粒度、默认值和独立性]
- [x] [已确认的字段来源、录入、显示和必填规则]
- [x] [已确认的空字符串、NULL、默认值和唯一性规则]
- [x] [已确认的历史数据、旧程序和未完成单据规则]

### 1.3 明确禁止

- [x] 不[错误实现或范围扩张]。
- [x] 不修改[排除模块/帐套/独立系统]。
- [x] 不把现有语言迁移为其他语言，除非需求明确要求。

---

## 2. 开发前确认与项目核查

### 2.1 已确认的业务选择

- [x] [字段长度、字符范围、默认值]
- [x] [历史数据方案]
- [x] [打印/导出范围]
- [x] [配置启用和关闭保护]

### 2.2 已核查的有效入口与排除项

- [x] 当前有效入口：[项目路径、菜单/路由/注册位置]。
- [x] 当前有效调用链：[入口 → 主流程 → 数据层]。
- [x] 纳入的公共控件、查询、流水和弹窗：[路径]。
- [x] 排除的未收录旧文件、重复实现和无调用代码：[路径及原因]。
- [x] 已核对旧程序 `INSERT` 与新增字段默认值的兼容性。

---

## 3. 系统配置

- [ ] 新增或复用配置项，明确编号、名称、类型、默认值和适用范围。
- [ ] 配置读取、缓存、重置和帐套切换行为完整。
- [ ] 配置界面独立显示、回显、保存和刷新。
- [ ] 配置启用/关闭保护按已确认规则实现。

---

## 4. 数据库升级

### 4.1 升级脚本与注册

- [ ] 新增符合项目命名和编码规范的升级脚本。
- [ ] 脚本可重复执行，默认约束、索引和视图均防重入。
- [ ] 在项目升级检测入口注册脚本或版本。
- [ ] 将脚本加入项目 `Content` 或现有发布包含机制。
- [ ] 升级检查验证配置、核心字段和关键明细字段，避免半升级。

### 4.2 表、字段、索引和视图

- [ ] [核心表] 增加 `[字段] [类型] NOT NULL DEFAULT ''`。
- [ ] 历史 `NULL` 和旧数据按已确认规则回填。
- [ ] 唯一键、普通索引、视图、联接和分组同步调整。
- [ ] 升级后执行空值、长度、重复键和数据守恒校验。

### 4.3 业务明细表

- [ ] [只列真实参与选择、占用、增减、冲销或成本处理的明细表]。
- [ ] 明细历史记录按兼容规则回填。
- [ ] 明确排除无活动入口的表和独立模块。

---

## 5. 有效数据访问链路

- [ ] 仅在有活动调用方的 Model/BLL/IDAL/DAL 中增加字段或方法。
- [ ] 现有 VB/DataTable/直接 SQL 在原语言和原调用链中修改。
- [ ] `Add`、`Update`、`GetModel`、映射、列表和自定义查询完整检查。
- [ ] SQL 参数类型、长度、空字符串和 `DBNull` 规则一致。
- [ ] DataTable 主键、缓存键、去重和合并条件同步调整。

---

## 6. 主业务流程

### 6.1 录入与回显

- [ ] 控件、列、显示、隐藏、必填和手工录入规则符合配置。
- [ ] 新增、修改和回显正确携带字段。
- [ ] 不新增项目不存在的复制、导入等功能。

### 6.2 保存、完成与撤销

- [ ] 保存持久化完整业务维度。
- [ ] 完成、撤销、返单和异常回滚精确命中同一业务对象。
- [ ] 事务边界与现有项目保持一致。
- [ ] 旧单据和旧程序数据按兼容规则继续处理。

---

## 7. 下游业务模块

### 7.1 [模块名称]

- [ ] 选择、保存、占用、完成、撤销和返单覆盖新增规则。
- [ ] 查询、联接、分组、价格或成本不会串到其他业务维度。

[按真实活动模块重复本小节，不存在的模块不要写。]

---

## 8. 查询、动态数据与流水

重点文件：

- `[当前有效查询入口]`
- `[动态查询/数据访问]`
- `[流水入口]`
- `[公共选择控件/弹窗]`

明确排除：

- `[未被项目收录的旧入口及原因]`

- [ ] 查询结果、筛选、分页、合计、导出和打印按新规则处理。
- [ ] 未完成业务占用量的 `GROUP BY` 和 `JOIN` 完整。
- [ ] 流水各分支输出一致字段并按正确维度筛选。
- [ ] 公共下拉、弹窗的主键和返回值完整。

---

## 9. 历史数据与配置切换

### 9.1 旧系统升级

- [ ] 新增字段回填、默认配置和旧程序兼容符合确认规则。
- [ ] 旧单据可查询、完成、撤销和返单。
- [ ] 升级前后核心数量、金额或状态保持一致。

### 9.2 配置启用

- [ ] 历史数据是否迁移、保留或标识按确认方案执行。
- [ ] 新程序新建/编辑数据执行启用后的校验。
- [ ] 多配置独立启用，不互相污染字段。

### 9.3 配置关闭

- [ ] 非零数据和未完成引用按确认方案阻止或迁移。
- [ ] 关闭一个配置不改变另一个配置。
- [ ] 禁止未经审核自动清空或合并数据。

---

## 10. 实施顺序

| 阶段 | 内容 | 完成标准 | 状态 |
|------|------|----------|------|
| 1 | 业务确认与项目入口核查 | 第 1～2 节完成 | 已完成/未开始 |
| 2 | 升级脚本、配置、核心表和视图 | 可重复升级且默认兼容 | 未开始 |
| 3 | 有效数据访问链路 | 字段可保存、读取和查询 | 未开始 |
| 4 | 主流程闭环 | 新增、完成、撤销正确 | 未开始 |
| 5 | 下游模块 | 所有业务精确处理 | 未开始 |
| 6 | 查询、流水和历史兼容 | 所有组合结果正确 | 未开始 |
| 7 | 联调验收 | 第 11 节通过 | 未开始 |

依赖关系：[明确先数据库/配置，再主流程，再下游，最后查询和验收。]

---

## 11. 验收清单

### 11.1 默认/旧模式

- [ ] 默认配置和升级前行为一致。
- [ ] 旧数据、旧程序和未完成单据可继续处理。
- [ ] 排除模块不受影响。

### 11.2 各独立配置组合

- [ ] [配置 A 开、B 关]。
- [ ] [配置 A 关、B 开]。
- [ ] [配置 A、B 都开]。
- [ ] [配置切换和关闭保护]。

### 11.3 数据与发布

- [ ] 完成和撤销前后核心数据守恒。
- [ ] 查询、合计、流水和公共选择结果一致。
- [ ] 升级脚本重复执行不报错、不重复创建对象。

---

## 12. 风险控制

1. [最可能遗漏的旧 SQL、入口或下游模块]。
2. [字段类型、默认值、NULL 和旧程序兼容风险]。
3. [配置组合、历史数据和切换风险]。
4. [排除模块和无入口代码误改风险]。
5. 本任务不新增测试项目或无关文档；按第 11 节执行项目既有验收方式。

